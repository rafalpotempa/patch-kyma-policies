apiVersion: policy/v1beta1
kind: PodSecurityPolicy
metadata:
  name: kyma.privileged
spec:
  privileged: true
  allowedCapabilities:
      - "*"
  seLinux:
    rule: RunAsAny
  supplementalGroups:
    rule: RunAsAny
  runAsUser:
    rule: RunAsAny
  fsGroup:
    rule: RunAsAny
  volumes:
  - '*'
